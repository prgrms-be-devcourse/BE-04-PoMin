spring:
  profiles:
    active: dev
  jpa:
    open-in-view: false
  config:
    import: sub/application-kakao.yml
---
spring:
  config:
    activate:
      on-profile: dev
    import: dev/application-db.yml, dev/application-jwt.yml
